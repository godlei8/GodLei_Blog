/.codex-tmp/
/.idea/
/release/
/.run/
/.vscode/

Thumbs.db
.DS_Store

# Node modules
node_modules/
*/node_modules/
*/*/node_modules/

# Twikoo local
/twikoo-local/node_modules/

# Environment files (keep .env.example if exists)
.env.local
.env.*.local

# 本地配置（含明文密钥，勿入库）
application-local.yml
application-local.yaml

# Build outputs
dist/
build/
out/

# 上传文件（本地存储）。迁移到 COS 并核对无误后，执行：
#   git rm -r --cached GodLeiBlog-Backend/blog-server/uploads
# 以停止跟踪（磁盘文件保留）。此忽略规则对已跟踪文件暂不生效。
GodLeiBlog-Backend/blog-server/uploads/

# IDE
*.iml

# Logs
*.log
logs/

# MongoDB
mongodb.zip

# Server key（注意：实际文件名为下划线；旧的 server-key.pem 连字符写法不生效）
server-key.pem
server_key.pem
*.pem
